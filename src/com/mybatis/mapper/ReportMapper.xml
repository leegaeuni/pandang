<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="report">
	<select id="selectAll" resultType="ReportVO" parameterType="map">
		select r.report_number, r.report_title, r.report_content, r.report_date, r.report_member_number
		, r.report_store_number, r.report_sns_number, m.member_id 
		from tbl_member m 
		join tbl_report r on m.member_number = r.report_member_number
		order by report_number desc
		limit #{startRow}, #{rowCount}
	</select>	
	
	<select id="getTotal" resultType="_int">
		select count(report_number) from tbl_report
	</select>

</mapper>